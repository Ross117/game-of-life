import React from 'react';

// child component of board
const Square = () => {
  return (
    <input type="button" className="square"/>
  );

//   define onclick event which updates board state
// will need to hook on to (2d) index of square
};

export default Square;
